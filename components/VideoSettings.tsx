
export type UserMedia = {
  videoDevice: string,
  audioDevice: string,
  width: number,
  height: number,
  framerate: number,
};

const VideoSettings = (props: any) => {
  
};

export default VideoSettings;
